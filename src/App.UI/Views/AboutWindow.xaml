<Window x:Class="AppUI.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Enterprise WPF Boilerplate" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding Version, StringFormat='Version {0}'}" FontSize="14" Margin="0,0,0,15"/>
            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" TextAlignment="Center"/>
            <Button Content="Close" IsCancel="True" Margin="0,20,0,0" Width="80" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
